services:
  # 1. Redis Service (消息隊列)
  redis:
    image: redis:8.4.0-alpine
    container_name: redis_pubsub
    expose:
      - "6379"

    command: redis-server --client-output-buffer-limit pubsub 128mb 64mb 60

    # 健康檢查確保其他服務等待 Redis 啟動
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 3s
      retries: 5
    
    restart: always

  # 🌟 2. Ollama LLM Service (翻譯引擎)
  ollama:
    build:
      context: .
      dockerfile: ollama/Dockerfile.ollama
    container_name: ollama_llm
    expose:
      - "11434"
    
    # 配置 GPU 支持 (Ollama 也需要 GPU)
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    
    environment:
      OLLAMA_HOST: "0.0.0.0:11434"
      NVIDIA_VISIBLE_DEVICES: all
    
    # 持久化模型，避免重複下載
    volumes:
      - ollama_models:/root/.ollama
    
    # 健康檢查 (使用 wget 或 ollama 命令)
    healthcheck:
      test: ["CMD-SHELL", "ollama list || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 60s
    
    restart: always

  # 3. Python Processor Service (Redis Subscriber/Publisher)
  processor:
    build:
      context: .
      dockerfile: processor/Dockerfile.processor
    container_name: python_processor
    
    # 配置 GPU 支持
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    
    environment:
      REDIS_HOST: redis 
      REDIS_PORT: 6379
      # 🌟 large-v3 最準確，large-v3-turbo 較快但略低準確度
      ASR_MODEL_NAME: large-v3
      MODEL_CACHE_DIR: /root/.cache/huggingface/hub
      HF_HUB_ENABLE_HF_TRANSFER: "0"
      # 🌟 NVIDIA 相關環境變數
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
      # 🌟 LLM 翻譯配置 (連接 Docker 內的 Ollama 服務)
      LLM_HOST: ollama
      LLM_PORT: "11434"
      LLM_MODEL: "qwen2.5:7b-instruct"
    
    volumes:
      - huggingface_cache:/root/.cache/huggingface
    
    # 等待 Ollama 服務就緒
    depends_on:
      redis:
        condition: service_healthy
      ollama:
        condition: service_healthy

    restart: always
      
  # 4. Node.js Server Service (FFmpeg, Redis I/O, WebSocket)
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    container_name: node_websocket_server
    # 對外暴露 WebSocket 端口
    ports:
      - "8080:8080" 
    depends_on:
      redis:
        condition: service_healthy
    # 配置 Redis 連線信息 (使用服務名稱 'redis')
    environment:
      REDIS_HOST: redis 
      REDIS_PORT: 6379

    restart: always

volumes:
  huggingface_cache:
    driver: local
  ollama_models:
    driver: local