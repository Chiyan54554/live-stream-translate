# ============================================================
# 🚀 剩餘小型套件（核心套件已在 Dockerfile 分層安裝）
# ============================================================

# 翻譯與工具
opencc-python-reimplemented>=0.1.7

# 雲端 ASR
google-cloud-speech>=2.26.0

# 異步 I/O
redis>=5.0.0
aiohttp>=3.9.0
async-timeout>=4.0.0

# 基礎（NumPy 1.x 與 stable-ts 相容）
numpy>=1.26.0,<2.0.0

# ============================================================
# ⚠️ 以下套件在 Dockerfile 中分層安裝（避免重複下載）：
# - torch==2.7.0 (cu128)
# - torchaudio==2.7.0 (cu128)
# - faster-whisper>=1.0.0
# - stable-ts>=2.16.0,<2.20.0
# - transformers>=4.40.0
# - accelerate>=0.30.0
# - punctuators>=0.0.5
# ============================================================