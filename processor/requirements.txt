# 🚀 大型套件（鎖定版本，利用快取）
# nvidia-cudnn-cu12==9.16.0.29

# ASR 核心
faster-whisper>=1.0.0
stable-ts>=2.16.0,<2.20.0

# Transformers (Kotoba-Whisper v2.2)
transformers>=4.40.0
accelerate>=0.30.0
punctuators>=0.0.5

# 翻譯與工具
opencc-python-reimplemented>=0.1.7

# 異步 I/O
redis>=5.0.0
aiohttp>=3.9.0

# 基礎（NumPy 1.x 與 stable-ts 相容）
numpy>=1.26.0,<2.0.0

# ⚠️ torch, torchaudio 在 Dockerfile 中單獨安裝（需要特定 CUDA 版本）
# ⚠️ 不要指定 nvidia-cudnn-cu12 版本，讓 torch 自動處理